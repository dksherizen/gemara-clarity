[project]
name = "gemara-clarity-py"
version = "0.1.0"
description = "Local-first Python pipeline for the Gemara Clarity PWA."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.7",
]

[tool.ruff]
line-length = 100
